import 'package:felloapp/ui/widgets/buttons/nav_buttons/nav_buttons.dart';
import 'package:flutter/material.dart';

class SellAssetsView extends StatelessWidget {
  const SellAssetsView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        leading: FelloAppBarBackButton(),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}

class _SelectAReasonBottomSheet extends StatelessWidget {
  const _SelectAReasonBottomSheet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class _SellingGoldAmountBottomSheet extends StatelessWidget {
  const _SellingGoldAmountBottomSheet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class _SaleConfirmationDialog extends StatelessWidget {
  const _SaleConfirmationDialog({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class _CountdownWidget extends StatelessWidget {
  const _CountdownWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
