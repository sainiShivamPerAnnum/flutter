import 'package:flutter/material.dart';

class TicketsHomeView extends StatelessWidget {
  const TicketsHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
